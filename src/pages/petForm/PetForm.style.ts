import { StyleSheet } from 'react-native';
import { CONST } from '../../constants/constants';

export default StyleSheet.create({
    container: {
        height: 100,
    },
    content: {
        backgroundColor: CONST.COLORS.LIB_GREY,
    },
});
